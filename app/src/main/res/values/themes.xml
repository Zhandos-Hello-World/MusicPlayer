<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/whiteDefaultColor</item>
        <item name="colorPrimaryVariant">@color/whiteDefaultColor</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="FrameLayout" parent="Theme.MyApplication">
        <item name="android:background">@color/white</item>
    </style>
    <style name="Controller" parent="Theme.MyApplication">
        <item name="android:background">@color/whiteDefaultColor</item>
    </style>
    <style name="About" parent="Controller">
        <item name="android:background">@color/whiteDefaultColor</item>
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="Picture">
        <item name="android:src">@drawable/white_disk</item>
    </style>

</resources>