<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/maroon</item>
        <item name="colorPrimaryVariant">@color/maroon</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="FrameLayout"  parent="Theme.MyApplication">
        <item name="android:background">@color/ttt</item>
    </style>
    <style name="Controller" parent="Theme.MyApplication">
        <item name="android:background">@color/black</item>
    </style>
    <style name="Picture">
        <item name="android:src">@drawable/disk</item>
    </style>

</resources>